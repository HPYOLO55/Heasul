import { CameraView, useCameraPermissions } from "expo-camera";
import { Link } from "expo-router";
import { Camera as CameraIcon } from "lucide-react-native";
import { Pressable, StyleSheet, Text, View } from "react-native";
export default function CameraScreen() { const [permission, requestPermission] = useCameraPermissions(); if (!permission?.granted) return <View style={styles.center}><CameraIcon color="#F5C542" size={48}/><Text style={styles.text}>Camera access is needed for AI analysis.</Text><Pressable style={styles.button} onPress={requestPermission}><Text style={styles.buttonText}>Allow Camera</Text></Pressable><Link href="/(tabs)/dashboard" style={styles.back}>Back to dashboard</Link></View>; return <View style={styles.container}><CameraView style={styles.camera} facing="front" /><View style={styles.controls}><Text style={styles.hint}>Position your face in the frame</Text><Pressable style={styles.capture}><View style={styles.captureInner}/></Pressable></View></View>; }
const styles = StyleSheet.create({ container: { flex: 1, backgroundColor: "#000" }, camera: { flex: 1 }, controls: { position: "absolute", bottom: 0, left: 0, right: 0, padding: 30, alignItems: "center", backgroundColor: "rgba(0,0,0,.45)" }, hint: { color: "#FFF", marginBottom: 20 }, capture: { width: 76, height: 76, borderRadius: 38, borderWidth: 5, borderColor: "#FFF", alignItems: "center", justifyContent: "center" }, captureInner: { width: 60, height: 60, borderRadius: 30, backgroundColor: "#F5C542" }, center: { flex: 1, backgroundColor: "#0D0D0D", alignItems: "center", justifyContent: "center", padding: 28 }, text: { color: "#FFF", textAlign: "center", marginVertical: 18 }, button: { backgroundColor: "#F5C542", padding: 15, borderRadius: 14 }, buttonText: { fontWeight: "800", color: "#17130A" }, back: { color: "#F5C542", marginTop: 20 } });
